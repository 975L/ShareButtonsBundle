{% trans_default_domain 'toolbar' %}

{% apply spaceless %}
{# Dashboard #}
{{ toolbar_button(path('sharebuttons_dashboard'), 'dashboard', 'md', 'label.user'|trans) }}

{# Config #}
{% if type != 'config' and is_granted(config('c975LUser.roleNeeded')) %}
    {{ toolbar_button(path('sharebuttons_config'), 'config') }}
{% endif %}

{# Help #}
{% if type != 'help' %}
    {{ toolbar_button(path('sharebuttons_help'), 'help') }}
{% endif %}

{# Signout #}
{{ toolbar_button(path('user_signout'), 'signout') }}

{# Other dashboards #}
{{ toolbar_dashboards('md') }}
{% endapply %}